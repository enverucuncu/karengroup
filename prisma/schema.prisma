model Project {
  id        Int     @id @default(autoincrement())
  slug      String  @unique
  coverUrl  String?
  titleTr   String?
  descTr    String?
  photos        Photo[]
  translations  ProjectTranslation[]
  @@map("projects") // (opsiyonel) tablo adı plural olabilir
}
